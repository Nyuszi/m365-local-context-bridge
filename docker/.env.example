# Absolute path to the primary project folder on the host (read-only mount)
PROJECT_HOST_PATH=/absolute/path/to/your/project

# Alias shown to Copilot (never a filesystem path)
BRIDGE_PRIMARY_ALIAS=project
